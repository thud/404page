export default class Chr {
    x: number;
    y: number;
    chr: string;

    constructor(x: number, y: number, chr: string) {
        this.x = x;
        this.y = y;
        this.chr = chr;
    }
}
